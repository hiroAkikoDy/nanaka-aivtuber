---
タスクID: 0006
依頼日時: 2026-05-12
ステップ: STEP6
---

## 目標
YouTube Liveのリアルタイムコメントを取得し、マルチペルソナAIVTuber（ナナカ・リョウ）が応答する配信システムを実装する。
OBS連携の準備として、テキストファイル出力機能を実装し、実際の配信環境でAIVTuberが動作できるようにする。

---

## 前提知識・参考情報

### 前プロジェクト「クレソンAI」での実装
- リポジトリ: https://github.com/hiroAkikoDy/hiroAkikoDy-watercress_cheff_ai
- YouTube Live連携実装済み
- 本プロジェクトでは同様の設計を改良して適用する

### 本プロジェクトでの位置づけ
- STEP1〜4: 単一AIVTuber（ナナカ）の基盤実装 ✅ 完了
- STEP5: 複数AIVTuberの対話実装 ✅ 完了
- STEP6: YouTube Live配信連携 ← 現在地
- 将来: OBS完全統合、配信自動化

### YouTube Data API v3について
- 公式ドキュメント: https://developers.google.com/youtube/v3
- 使用エンドポイント: `liveChatMessages.list`
- 認証方式: OAuth 2.0
- API クォータ: 1日あたり10,000ユニット（liveChatMessages.list = 5ユニット/リクエスト）

---

## YouTube API 準備手順

### 1. Google Cloud Console での設定

#### 1-1. プロジェクト作成
1. https://console.cloud.google.com/ にアクセス
2. 新しいプロジェクトを作成（例: nanaka-aivtuber）
3. プロジェクトIDをメモ

#### 1-2. YouTube Data API v3 の有効化
1. 「APIとサービス」→「ライブラリ」
2. "YouTube Data API v3" を検索
3. 「有効にする」をクリック

#### 1-3. OAuth 2.0 認証情報の作成
1. 「APIとサービス」→「認証情報」
2. 「認証情報を作成」→「OAuthクライアントID」
3. アプリケーションの種類: 「デスクトップアプリ」
4. 名前: "nanaka-aivtuber-client"
5. JSONファイルをダウンロード → `credentials/client_secret.json` として保存

#### 1-4. OAuth同意画面の設定
1. 「OAuth同意画面」を設定
2. ユーザータイプ: 「外部」（テスト用）
3. スコープ: `https://www.googleapis.com/auth/youtube.readonly`
4. テストユーザー: 自分のGoogleアカウントを追加

---

## 作業内容

### 1. プロジェクト構成の拡張

#### 1-1. 必要なライブラリの追加
`requirements.txt` に以下を追加：
```txt
# YouTube API
google-api-python-client==2.147.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1
```

インストール:
```bash
pip install -r requirements.txt
```

#### 1-2. ディレクトリ構成の追加
```
nanaka-aivtuber/
├── agents/
│   ├── youtube_agent.py          # 新規: YouTube コメント取得エージェント
│   └── ...
├── core/
│   ├── run_live.py                # 新規: 配信モード実行スクリプト
│   └── ...
├── credentials/                    # 新規: 認証情報ディレクトリ
│   ├── client_secret.json         # OAuth 2.0 クライアント情報
│   └── token.json                 # アクセストークン（自動生成）
├── output/                         # 新規: OBS用出力ディレクトリ
│   ├── current_speaker.txt        # 現在の発言者名
│   └── current_text.txt           # 現在の発言テキスト
└── config/
    └── settings.yaml              # YouTube設定を追加
```

#### 1-3. `.gitignore` の更新
認証情報を除外：
```gitignore
# YouTube API 認証情報
credentials/client_secret.json
credentials/token.json

# OBS出力ファイル
output/*.txt
```

---

### 2. `config/settings.yaml` の拡張

YouTube Live設定を追加：
```yaml
# 既存の設定...

youtube:
  enabled: true
  mode: "live"  # live: リアルタイム配信モード / dummy: ダミーモード

  # OAuth 2.0 認証
  credentials_path: "credentials/client_secret.json"
  token_path: "credentials/token.json"

  # 配信情報
  video_id: null  # 配信開始時に自動取得（または手動設定）

  # ポーリング設定
  polling_interval: 5  # 秒単位（YouTube推奨: 5秒以上）
  max_results: 10      # 1回のリクエストで取得する最大コメント数

  # レート制限対策
  quota_limit_per_day: 10000  # YouTube API クォータ上限
  quota_warning_threshold: 8000  # 警告を出す閾値

# OBS連携設定
obs:
  enabled: true
  output_dir: "output"
  speaker_file: "current_speaker.txt"
  text_file: "current_text.txt"
  max_text_length: 100  # OBSに表示する最大文字数
```

---

### 3. `agents/youtube_agent.py` の作成

YouTube Live コメントを取得するエージェント：

```python
"""
YouTube Live コメント取得エージェント

YouTube Data API v3を使用してリアルタイムコメントを取得し、
重複排除と処理状態の管理を行う。
"""

import os
import time
from typing import Optional, List, Dict
from google.auth.transport.requests import Request
from google.oauth2.credentials import Credentials
from google_auth_oauthlib.flow import InstalledAppFlow
from googleapiclient.discovery import build
from googleapiclient.errors import HttpError
import yaml


class YouTubeAgent:
    """YouTube Live コメント取得エージェント"""

    SCOPES = ['https://www.googleapis.com/auth/youtube.readonly']

    def __init__(self, config_path: str = "config/settings.yaml"):
        """
        Args:
            config_path: 設定ファイルのパス
        """
        # 設定読み込み
        with open(config_path, "r", encoding="utf-8") as f:
            self.config = yaml.safe_load(f)

        self.youtube_config = self.config.get("youtube", {})

        # 認証情報パス
        self.credentials_path = self.youtube_config.get("credentials_path", "credentials/client_secret.json")
        self.token_path = self.youtube_config.get("token_path", "credentials/token.json")

        # YouTube API クライアント
        self.youtube = None

        # 配信情報
        self.video_id = self.youtube_config.get("video_id")
        self.live_chat_id = None

        # ポーリング設定
        self.polling_interval = self.youtube_config.get("polling_interval", 5)
        self.max_results = self.youtube_config.get("max_results", 10)

        # 処理済みコメントID（重複防止）
        self.processed_comment_ids = set()

        # 次回ポーリング用トークン
        self.next_page_token = None

        # API クォータ管理
        self.quota_used = 0
        self.quota_limit = self.youtube_config.get("quota_limit_per_day", 10000)
        self.quota_warning_threshold = self.youtube_config.get("quota_warning_threshold", 8000)

    def authenticate(self):
        """OAuth 2.0 認証を実行"""
        creds = None

        # token.json が存在する場合は読み込み
        if os.path.exists(self.token_path):
            creds = Credentials.from_authorized_user_file(self.token_path, self.SCOPES)

        # 認証情報がないか無効な場合は再認証
        if not creds or not creds.valid:
            if creds and creds.expired and creds.refresh_token:
                creds.refresh(Request())
            else:
                flow = InstalledAppFlow.from_client_secrets_file(
                    self.credentials_path, self.SCOPES
                )
                creds = flow.run_local_server(port=0)

            # token.json を保存
            with open(self.token_path, "w") as token:
                token.write(creds.to_json())

        # YouTube API クライアント構築
        self.youtube = build("youtube", "v3", credentials=creds)
        print("[YouTubeAgent] 認証完了")

    def get_live_chat_id(self, video_id: Optional[str] = None) -> Optional[str]:
        """
        配信のライブチャットIDを取得

        Args:
            video_id: 動画ID（Noneの場合は自動検出）

        Returns:
            str: ライブチャットID
        """
        if video_id is None:
            video_id = self._get_active_live_video_id()
            if video_id is None:
                print("[YouTubeAgent] アクティブな配信が見つかりません")
                return None

        try:
            request = self.youtube.videos().list(
                part="liveStreamingDetails",
                id=video_id
            )
            response = request.execute()
            self._update_quota(1)  # videos.list = 1 unit

            if response["items"]:
                live_chat_id = response["items"][0]["liveStreamingDetails"]["activeLiveChatId"]
                print(f"[YouTubeAgent] ライブチャットID取得: {live_chat_id}")
                return live_chat_id
            else:
                print(f"[YouTubeAgent] 動画ID {video_id} が見つかりません")
                return None

        except HttpError as e:
            print(f"[YouTubeAgent] エラー: {e}")
            return None

    def _get_active_live_video_id(self) -> Optional[str]:
        """
        現在アクティブな配信の動画IDを自動検出

        Returns:
            str: 動画ID
        """
        try:
            request = self.youtube.liveBroadcasts().list(
                part="id,snippet",
                broadcastStatus="active",
                maxResults=1
            )
            response = request.execute()
            self._update_quota(1)  # liveBroadcasts.list = 1 unit

            if response["items"]:
                video_id = response["items"][0]["id"]
                title = response["items"][0]["snippet"]["title"]
                print(f"[YouTubeAgent] アクティブな配信検出: {title} (ID: {video_id})")
                return video_id
            else:
                return None

        except HttpError as e:
            print(f"[YouTubeAgent] エラー: {e}")
            return None

    def fetch_comments(self) -> List[Dict[str, str]]:
        """
        ライブチャットからコメントを取得

        Returns:
            list[dict]: [
                {"author": "ユーザー名", "text": "コメント内容", "id": "コメントID"},
                ...
            ]
        """
        if self.youtube is None:
            print("[YouTubeAgent] 認証が必要です。先にauthenticate()を実行してください")
            return []

        if self.live_chat_id is None:
            print("[YouTubeAgent] ライブチャットIDが設定されていません")
            return []

        try:
            request = self.youtube.liveChatMessages().list(
                liveChatId=self.live_chat_id,
                part="snippet,authorDetails",
                maxResults=self.max_results,
                pageToken=self.next_page_token
            )
            response = request.execute()
            self._update_quota(5)  # liveChatMessages.list = 5 units

            # 次回ポーリング用トークン更新
            self.next_page_token = response.get("nextPageToken")
            polling_interval_ms = response.get("pollingIntervalMillis", self.polling_interval * 1000)

            # コメント抽出
            new_comments = []
            for item in response.get("items", []):
                comment_id = item["id"]

                # 重複チェック
                if comment_id in self.processed_comment_ids:
                    continue

                author = item["authorDetails"]["displayName"]
                text = item["snippet"]["displayMessage"]

                new_comments.append({
                    "author": author,
                    "text": text,
                    "id": comment_id
                })

                # 処理済みとしてマーク
                self.processed_comment_ids.add(comment_id)

            return new_comments

        except HttpError as e:
            print(f"[YouTubeAgent] エラー: {e}")
            return []

    def _update_quota(self, units: int):
        """
        API クォータ使用量を更新

        Args:
            units: 消費したユニット数
        """
        self.quota_used += units

        if self.quota_used >= self.quota_warning_threshold:
            print(f"[YouTubeAgent] 警告: クォータ使用量 {self.quota_used}/{self.quota_limit}")

        if self.quota_used >= self.quota_limit:
            print(f"[YouTubeAgent] エラー: クォータ上限に達しました ({self.quota_used}/{self.quota_limit})")

    def start_monitoring(self, video_id: Optional[str] = None):
        """
        配信モニタリングを開始

        Args:
            video_id: 動画ID（Noneの場合は自動検出）
        """
        # 認証
        self.authenticate()

        # ライブチャットID取得
        self.live_chat_id = self.get_live_chat_id(video_id)

        if self.live_chat_id is None:
            print("[YouTubeAgent] モニタリングを開始できません")
            return

        print(f"[YouTubeAgent] モニタリング開始（ポーリング間隔: {self.polling_interval}秒）")


# テスト用コード
if __name__ == "__main__":
    agent = YouTubeAgent()

    # 認証とモニタリング開始
    agent.start_monitoring()

    # コメント取得テスト（10回）
    for i in range(10):
        print(f"\n=== ポーリング {i+1} ===")
        comments = agent.fetch_comments()

        if comments:
            for comment in comments:
                print(f"[{comment['author']}] {comment['text']}")
        else:
            print("新しいコメントなし")

        time.sleep(agent.polling_interval)
```

---

### 4. `core/run_live.py` の作成

配信モード実行スクリプト：

```python
"""
YouTube Live 配信モード実行スクリプト

リアルタイムでYouTube Liveコメントを取得し、
マルチペルソナAIVTuberが応答する。
"""

import time
import os
import sys
import yaml

# パスの追加（agents, coreモジュールをインポート可能にする）
sys.path.append(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

from agents.youtube_agent import YouTubeAgent
from core.aituber_system import AITuberSystem


class LiveStreamingSystem:
    """YouTube Live 配信システム"""

    def __init__(self, config_path: str = "config/settings.yaml"):
        """
        Args:
            config_path: 設定ファイルのパス
        """
        # 設定読み込み
        with open(config_path, "r", encoding="utf-8") as f:
            self.config = yaml.safe_load(f)

        # YouTube Agent
        self.youtube_agent = YouTubeAgent(config_path)

        # AITuber System
        self.aituber_system = AITuberSystem()

        # OBS設定
        self.obs_config = self.config.get("obs", {})
        self.obs_enabled = self.obs_config.get("enabled", True)
        self.output_dir = self.obs_config.get("output_dir", "output")
        self.speaker_file = self.obs_config.get("speaker_file", "current_speaker.txt")
        self.text_file = self.obs_config.get("text_file", "current_text.txt")
        self.max_text_length = self.obs_config.get("max_text_length", 100)

        # 出力ディレクトリ作成
        os.makedirs(self.output_dir, exist_ok=True)

    def update_obs_display(self, speaker: str, text: str):
        """
        OBS用テキストファイルを更新

        Args:
            speaker: 発言者名
            text: 発言テキスト
        """
        if not self.obs_enabled:
            return

        # 文字数制限
        if len(text) > self.max_text_length:
            text = text[:self.max_text_length] + "..."

        # ファイル出力
        speaker_path = os.path.join(self.output_dir, self.speaker_file)
        text_path = os.path.join(self.output_dir, self.text_file)

        with open(speaker_path, "w", encoding="utf-8") as f:
            f.write(speaker)

        with open(text_path, "w", encoding="utf-8") as f:
            f.write(text)

        print(f"[OBS] 更新: {speaker} - {text}")

    def clear_obs_display(self):
        """OBS表示をクリア"""
        self.update_obs_display("", "")

    def run(self, video_id: str = None):
        """
        配信モードを実行

        Args:
            video_id: YouTube動画ID（Noneの場合は自動検出）
        """
        print("=" * 50)
        print("YouTube Live 配信モード起動")
        print("=" * 50)

        # YouTube監視開始
        self.youtube_agent.start_monitoring(video_id)

        if self.youtube_agent.live_chat_id is None:
            print("配信が見つかりません。終了します。")
            return

        print("\n配信モニタリング開始...")
        print("（Ctrl+C で終了）\n")

        try:
            while True:
                # コメント取得
                comments = self.youtube_agent.fetch_comments()

                # コメント処理
                for comment in comments:
                    viewer_name = comment["author"]
                    comment_text = comment["text"]

                    print(f"\n[視聴者] {viewer_name}: {comment_text}")

                    # OBS表示更新（視聴者コメント）
                    self.update_obs_display(viewer_name, comment_text)
                    time.sleep(2)  # 視聴者コメント表示時間

                    # AITuberが応答（マルチペルソナモード対応）
                    if self.config.get("multi_persona", {}).get("enabled", False):
                        # マルチペルソナモード
                        dialogue = self.aituber_system.get_dialogue_response(viewer_name, comment_text)

                        for turn in dialogue:
                            persona = turn["persona"]
                            text = turn["text"]

                            print(f"[{persona}] {text}")

                            # OBS表示更新（AIVTuber発言）
                            self.update_obs_display(persona, text)

                            # 音声再生完了まで待機（実際の音声長に応じて調整）
                            time.sleep(len(text) * 0.3)  # 仮の待機時間
                    else:
                        # 単独モード
                        self.aituber_system.talk_with_comment(viewer_name, comment_text)
                        # 最後の応答を取得（talk_with_commentの戻り値を拡張する必要あり）
                        # 簡易実装: 固定メッセージ
                        self.update_obs_display("ナナカ", "応答を生成しました")

                    # OBS表示クリア
                    self.clear_obs_display()

                # ポーリング間隔待機
                time.sleep(self.youtube_agent.polling_interval)

        except KeyboardInterrupt:
            print("\n\n配信モードを終了します")
            self.clear_obs_display()


# ダミーモード（YouTube APIを使わずに動作確認）
class DummyLiveStreamingSystem(LiveStreamingSystem):
    """ダミー配信システム（テスト用）"""

    def run(self, video_id: str = None):
        """ダミーコメントで動作確認"""
        print("=" * 50)
        print("ダミー配信モード起動")
        print("=" * 50)

        dummy_comments = [
            {"author": "太郎", "text": "クレソンってどんな味なの？"},
            {"author": "花子", "text": "クレソンを使ったおすすめレシピ教えて！"},
            {"author": "次郎", "text": "クレソンの栽培って難しいの？"},
        ]

        print("\nダミーコメント処理開始...\n")

        for comment in dummy_comments:
            viewer_name = comment["author"]
            comment_text = comment["text"]

            print(f"\n[視聴者] {viewer_name}: {comment_text}")
            self.update_obs_display(viewer_name, comment_text)
            time.sleep(2)

            # AITuber応答
            self.aituber_system.talk_with_comment(viewer_name, comment_text)

            self.clear_obs_display()
            time.sleep(3)

        print("\nダミーモード完了")


if __name__ == "__main__":
    # 設定読み込み
    with open("config/settings.yaml", "r", encoding="utf-8") as f:
        config = yaml.safe_load(f)

    youtube_mode = config.get("youtube", {}).get("mode", "dummy")

    if youtube_mode == "live":
        # リアルタイム配信モード
        system = LiveStreamingSystem()
        system.run()
    else:
        # ダミーモード
        system = DummyLiveStreamingSystem()
        system.run()
```

---

### 5. `core/aituber_system.py` の拡張

マルチペルソナの応答を外部から取得できるように拡張：

```python
# 既存のコード...

def get_dialogue_response(self, viewer_name: str, comment: str) -> list[dict]:
    """
    マルチペルソナモードの会話を取得（音声再生なし）

    Args:
        viewer_name: 視聴者名
        comment: コメント内容

    Returns:
        list[dict]: [
            {"persona": "ナナカ", "text": "...", "speaker_id": 46},
            {"persona": "リョウ", "text": "...", "speaker_id": 3},
            ...
        ]
    """
    if not self.multi_persona_enabled:
        return []

    # 記憶取得
    memory = self.memory_agent.get_memory(viewer_name)

    # 感情分析
    emotion_result = self.emotion_agent.analyze(comment)
    emotion = emotion_result["emotion"]

    # 会話生成
    dialogue = self.dialogue_coordinator.coordinate_dialogue(comment, emotion)

    # 記憶保存（最後のナナカの発言）
    nanaka_turns = [t for t in dialogue if t['persona'] == 'nanaka']
    if nanaka_turns:
        last_nanaka_response = nanaka_turns[-1]['text']
        self.memory_agent.save_interaction(viewer_name, comment, emotion, last_nanaka_response)

    return dialogue
```

この変更により、`run_live.py` から会話内容を取得してOBS出力に利用できる。

---

### 6. OBS連携設定

#### 6-1. OBSでのテキストソース追加
1. OBSを起動
2. ソース追加 → 「テキスト（GDI+）」
3. ソース名: "AIVTuber発言者"
4. 「ファイルからテキストを読み込む」にチェック
5. ファイルパス: `C:\Users\...\nanaka-aivtuber\output\current_speaker.txt`
6. フォント・色・サイズを調整

同様に、`current_text.txt` 用のテキストソースも追加。

#### 6-2. 表示レイアウト例
```
┌─────────────────────────────┐
│  AIVTuber映像（背景・立ち絵）│
│                               │
│  ┌─────────────────────┐   │
│  │ ナナカ               │   │  ← current_speaker.txt
│  │ クレソンはね、水が  │   │  ← current_text.txt
│  │ きれいだと美味しく  │   │
│  │ 育つんだよ！         │   │
│  └─────────────────────┘   │
└─────────────────────────────┘
```

---

### 7. 動作確認

#### 7-1. YouTube API 認証テスト
```bash
cd agents
python youtube_agent.py
```

**確認ポイント**:
- ブラウザが開き、Googleアカウント認証が行われる
- 「nanaka-aivtuber-client がアクセスを求めています」と表示される
- 許可すると `credentials/token.json` が生成される
- アクティブな配信がない場合は「アクティブな配信が見つかりません」と表示される

#### 7-2. ダミーモードでの動作確認
`config/settings.yaml` で `youtube.mode: "dummy"` に設定：
```bash
cd core
python run_live.py
```

**確認ポイント**:
- ダミーコメント3件が処理される
- `output/current_speaker.txt` と `output/current_text.txt` が更新される
- マルチペルソナモードが有効な場合、ナナカとリョウが交互に話す
- 音声が再生される

#### 7-3. リアルタイム配信モードでの動作確認
1. YouTube Studioで配信を開始（テスト配信でOK）
2. `config/settings.yaml` で `youtube.mode: "live"` に設定
3. `youtube.video_id` にYouTube動画IDを設定（または自動検出）
4. 実行：
```bash
cd core
python run_live.py
```

**確認ポイント**:
- 配信のライブチャットIDが取得される
- リアルタイムでコメントが取得される
- コメントに対してAIVTuberが応答する
- OBSのテキストソースが更新される
- 重複コメントが処理されない

#### 7-4. OBS連携確認
1. OBSで `output/*.txt` をテキストソースとして読み込み
2. `run_live.py` を実行
3. OBS画面でテキストが更新されることを確認

---

## 完了条件

- [ ] `requirements.txt` に `google-api-python-client` 等が追加されている
- [ ] `credentials/` ディレクトリが作成され、`.gitignore` に追加されている
- [ ] `output/` ディレクトリが作成され、`.gitignore` に追加されている
- [ ] `config/settings.yaml` に `youtube` と `obs` 設定が追加されている
- [ ] `agents/youtube_agent.py` が存在し、コメント取得機能が動作する
- [ ] `core/run_live.py` が存在し、ダミーモードとライブモードが切り替え可能
- [ ] `core/aituber_system.py` に `get_dialogue_response()` メソッドが追加されている
- [ ] YouTube API 認証（OAuth 2.0）が成功する
- [ ] ダミーモードでコメント処理と音声再生が成功する
- [ ] リアルタイムモードでYouTube Liveコメントが取得できる
- [ ] OBS用テキストファイルが正しく更新される
- [ ] 重複コメントが処理されない
- [ ] API クォータ管理が機能する
- [ ] `task_queue/done_0006.md` を作成する
- [ ] `work_reports/WORK_REPORT_YYYYMMDD_HHMM.md` に変更ファイル一覧を記載する

---

## 参照ファイル

- `AGENTS.md` — プロジェクト全体方針
- 前プロジェクト「クレソンAI」リポジトリ — YouTube Live連携実装
- `config/settings.yaml` — 設定ファイル（拡張対象）
- `core/aituber_system.py` — 統合対象
- `agents/persona_agent.py` — マルチペルソナ（STEP5で実装）
- `agents/dialogue_coordinator.py` — 会話調整（STEP5で実装）
- YouTube Data API v3 公式ドキュメント: https://developers.google.com/youtube/v3

---

## 注意事項

### YouTube API クォータ管理
- **1日の上限**: 10,000ユニット
- **liveChatMessages.list**: 5ユニット/リクエスト
- **計算例**: 5秒間隔で12時間配信 = 8,640リクエスト = 43,200ユニット → 上限超過
- **対策**: ポーリング間隔を調整（推奨: 10〜15秒）

### 認証情報の安全管理
- `client_secret.json` と `token.json` を **絶対にGitにコミットしない**
- `.gitignore` に確実に追加
- 公開リポジトリの場合は特に注意

### ポーリング間隔の調整
- YouTube推奨: 5秒以上
- クォータ節約: 10〜15秒
- レート制限: 1秒あたり3リクエストまで

### OBSテキスト表示の最適化
- 長文コメントは省略表示
- 日本語フォントを使用（MSゴシック、Yu Gothic等）
- 背景色を調整して視認性向上

### エラー処理
- API認証エラー: 再認証フローを実行
- 配信が見つからない: video_idを手動設定
- クォータ超過: ポーリング間隔を延長

### 将来の拡張性
- STEP7以降: スーパーチャット対応、感情に応じた立ち絵変更
- 視聴者との長期記憶連携（Neo4j）
- 自動配信開始・終了機能

---

## デバッグのヒント

### 認証がうまくいかない場合
- `credentials/token.json` を削除して再認証
- Google Cloud Consoleで「OAuth同意画面」のテストユーザーを確認
- スコープが正しいか確認（`youtube.readonly`）

### コメントが取得できない場合
- 配信が実際にアクティブか確認（YouTube Studio）
- `video_id` が正しいか確認（URLの `v=` の後）
- ライブチャットが有効か確認（配信設定）

### 重複コメントが処理される場合
- `processed_comment_ids` に正しくIDが追加されているか確認
- ログに `[DEBUG] Comment ID: {id}` を出力して追跡

### OBSでテキストが表示されない場合
- ファイルパスが正しいか確認（絶対パス推奨）
- ファイルの文字コードが UTF-8 か確認
- OBSのテキストソースで「ファイルからテキストを読み込む」にチェック

### API クォータがすぐに上限に達する場合
- `polling_interval` を延長（10秒以上推奨）
- `max_results` を減らす（5件程度）
- 不要なAPIリクエストを削減

---

## 学習ポイント

このタスクで学べること：

| 項目 | 内容 |
|------|------|
| **YouTube Data API v3** | OAuth 2.0認証、ライブチャット取得 |
| **リアルタイム処理** | ポーリング方式、重複排除 |
| **外部API連携** | レート制限、クォータ管理 |
| **OBS連携** | テキストファイル出力、リアルタイム更新 |
| **システム統合** | 既存機能（マルチペルソナ）との連携 |

STEP7以降では、このYouTube Live連携機能を拡張し、スーパーチャット対応や感情に応じた立ち絵変更を実装する予定。
