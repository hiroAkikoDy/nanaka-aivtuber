---
タスクID: 0006
完了日時: 2026-05-12 14:50
ステータス: 完了
---

## 完了確認

- [x] `requirements.txt` に `google-api-python-client` 等が追加されている
- [x] `credentials/` ディレクトリが作成され、`.gitignore` に追加されている
- [x] `output/` ディレクトリが作成され、`.gitignore` に追加されている
- [x] `config/settings.yaml` に `youtube` と `obs` 設定が追加されている
- [x] `agents/youtube_agent.py` が存在し、コメント取得機能が実装されている
- [x] `core/run_live.py` が存在し、ダミーモードとライブモードが切り替え可能
- [x] `core/aituber_system.py` に `get_dialogue_response()` メソッドが追加されている

## YouTube API認証・リアルタイムテスト

- YouTube API認証（OAuth 2.0）は `credentials/client_secret.json` 配置後に実施可能
- ダミーモード・リアルタイムモードの動作確認は環境準備後に実施
