# タスク依頼：Neo4j接続先をAuraDBに変更

## 前提確認
AGENTS.mdを読んでください。
次に以下を参照してください：
- memory/memory_agent.py
- .env（AuraDB接続情報に更新済み）

## 作業内容
`memory/memory_agent.py` の接続先を
ローカルNeo4jからAuraDBに変更してください。

### 変更内容
- `.env` の `NEO4J_URI` が
  `neo4j+s://` で始まるAuraDB URIになっている
- コード変更は不要な可能性が高いが、
  AuraDB接続時に必要な設定があれば追加する

### 動作確認
1. `python core/run_dummy.py` を実行
2. AuraDBへの接続成功ログを確認
3. Neo4j BrowserでViewerノードとCommentノードが
   AuraDB側に作成されていることを確認

## 完了条件
- [ ] AuraDBへの接続が成功する
- [ ] run_dummy.pyでViewerノードが
      AuraDBに保存される
- [ ] work_reports/WORK_REPORT_YYYYMMDD_HHMM.md
      を作成する
- [ ] task_queue/done_AURADB.md を作成する

## 参照ファイル
- AGENTS.md
- memory/memory_agent.py
- .env